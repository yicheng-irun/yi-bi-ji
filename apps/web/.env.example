# Vite dev server 监听地址
# - 默认 '0.0.0.0'：监听所有网络接口（IPv4），同一局域网内其他设备可访问
# - 'true'：Vite 原生 host: true 模式，同时监听 IPv4 和 IPv6（等同于 '::'）
# - ''（空/不设置）：兜底为 '0.0.0.0'
# - 具体 IP 如 '127.0.0.1'：仅本机可访问
WEB_DEV_HOST=0.0.0.0
